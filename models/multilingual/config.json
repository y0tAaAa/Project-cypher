{
  "model_name": "gpt2",
  "training_args": {
    "num_train_epochs": 3,
    "per_device_train_batch_size": 4,
    "per_device_eval_batch_size": 4,
    "gradient_accumulation_steps": 2,
    "learning_rate": 5e-05,
    "weight_decay": 0.01,
    "warmup_steps": 500,
    "eval_steps": 500,
    "save_steps": 1000,
    "logging_steps": 100,
    "fp16": true
  }
}